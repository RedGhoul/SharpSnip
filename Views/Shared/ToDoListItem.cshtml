@model Snips.Data.ToDoListItem

    <div class="form-group">
        <div class="col-md-10">
            @Html.HiddenFor(model => model.Id)
            @Html.EditorFor(model => model.Content, new { htmlAttributes = new { @class = "form-control" } })
        </div>
    </div>
